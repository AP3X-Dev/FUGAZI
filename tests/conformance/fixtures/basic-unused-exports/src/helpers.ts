export function usedHelper(label: string): string {
  return `[${label}]`;
}

export function unusedHelperA(): number {
  return 1;
}

export function unusedHelperB(): number {
  return 2;
}

export const unusedConst = 42;
