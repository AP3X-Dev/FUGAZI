{
  "name": "@fugazi/test-corpus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Test corpus: conformance fixtures, project fixtures, ecosystem regression workflow, regression fixtures, property tests.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "@fugazi/config": "workspace:*",
    "@fugazi/core": "workspace:*",
    "@fugazi/types": "workspace:*"
  },
  "devDependencies": {
    "@fast-check/vitest": "0.1.3",
    "fast-check": "^4.7.0",
    "vitest": "2.1.8"
  }
}
